- name: Red-hat updates
  hosts: all
  tasks:
     - name: Updating system
       command: wall updating system
       register: wall
     - name: Patching command
       command: yum -y update
       register: update
    - debug: msg="{{ wall.stdout }}"
    - debug: msg="{{ update.stdout }}"
